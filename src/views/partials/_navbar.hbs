<nav class='navbar navbar-dark bg-dark navbar-expand-lg'>
  <div class='container-fluid'>
    <a href='/list/dog/images' class='navbar-brand'>
      Dogs
    </a>
    <button
      class='navbar-toggler'
      type='button'
      data-bs-toggle='collapse'
      data-bs-target='#navbarNav'
      aria-controls='navbarNav'
      aria-expanded='false'
      aria-label='Toggle navigation'
    >
      <span class='navbar-toggler-icon'></span>
    </button>
    <div class='collapse navbar-collapse' id='navbarNav'>
      <ul class='navbar-nav'>
        <li class='nav-item'>
          {{#if isHome}}
            <a class='nav-link active' aria-current='page' href='/list/dog/images'>
              Home
            </a>
          {{else}}
            <a class='nav-link' aria-current='page' href='/list/dog/images'>
              Home
            </a>
          {{/if}}
        </li>
        {{#if isCreate}}
          <a class='nav-link active' aria-current='page' href='/upload/dog'>
            Create
          </a>
        {{else}}
          <a class='nav-link' aria-current='page' href='/upload/dog'>
            Create
          </a>
        {{/if}}
      </ul>
    </div>
  </div>
</nav>