
# Chatbots-studio-test-task

## Installation

```bash
npm i
# then create .env from .env.example template
```

## Usage

```bash
npm run dev

# or

npm run build
npm run start
```

## Task
```
Тестове завдання:
Засетити Typescript проект з eslint та базою даних. База даних довільна. Для eslint рекомендуємо використовувати плагін airbnb.
Використати TS інтерфейси та enums там, де це можливо та потрібно.
\*Якщо ви ніколи не працювали з Typescript - Курс за 70 хв

Створити сервер на Express.js з 2 роутами:

1. POST /upload/dog/image:
   генерує рандомну картинку тварини через АПІ https://random.dog/woof.json
   (це апі може вертати відео - їх потрібно пропускати)
   змінює розміри картинки з отриманими в ріквесті resize параметрами
   зберігає інфу у базу даних
2. GET /list/dog/images
   приймає параметри для пошуку (фільтрування) та вертає всі картинки, що їх задовольняють

Від кандидата очікується:

1. Робочий проект, у якому:
2. Демонстрація роботи з гітом та історія комітів:

- не повинно бути ситуації, що весь проект було залито через 1 коміт - як мінімум 5
- коміт повідомлення мають мати сенс (не допускається: “test”, “fix”, “123”, “aaaa”,
  “works now” тощо); Гайд як вести хороші коміт меседжі
  Куди відправити виконане завдання:
  Після завершення тестового завдання результат треба надіслати через цю гугл-форму.
  Будь ласка, вказуй ту ж пошту, що ти використовував(ла) для заповнення попередньої (реєстраційної) форми, щоб ми змогли тебе ідентифікувати.

Я виконав(ла) та відправив(ла) через гугл форму. Що далі?
Після відправки тестового у гугл формах ти побачиш дату, до котрої тобі повідомлять результат мейлом або дзвінком на номер телефону, який ти зазначив при реєстрації.

Важливо: кожного стажування ми відбираємо технічно найсильніші проекти і пропонуємо деяким кандидатам одразу пройти (як мінімум) технічну співбесіду на позицію Junior (тобто без участі у стажуванні).

```
